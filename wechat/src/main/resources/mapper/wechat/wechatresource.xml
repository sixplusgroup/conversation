<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.wechat.resource">
    <resultMap id="resourceVo" type="finley.gmair.model.wechat.WechatResource">
        <result property="resourceId" column="resource_id"></result>
        <result property="resourceName" column="resource_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="resourceVo">
        SELECT resource_id, resource_name, block_flag, create_time
        FROM gmair_wechat.wechat_resource
        WHERE 1 = 1
        <if test="resourceId != null">
            AND resource_id = #{resourceId}
        </if>
        <if test="resourceName != null">
            AND resource_name = #{resourceName}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="finley.gmair.model.wechat.WechatResource" useGeneratedKeys="false">
        INSERT INTO gmair_wechat.wechat_resource(resource_id, resource_name, block_flag, create_time)
        VALUES (#{resourceId}, #{resourceName}, #{blockFlag}, #{createAt})
    </insert>

    <update id="update" parameterType="finley.gmair.model.wechat.WechatResource">
        UPDATE gmair_wechat.wechat_resource
        <set>
            <if test="resourceId != null">
                resource_id = #{resourceId},
            </if>
        </set>
        WHERE resource_name = #{resourceName}
    </update>
</mapper>