<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.wechat.messagetemplate">
    <resultMap id="MessageTemplateVo" type="finley.gmair.model.wechat.MessageTemplate">
        <result property="templateId" column="template_id"></result>
        <result property="messageType" column="message_type"></result>
        <result property="messageContent" column="message_content"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.wechat.MessageTemplate">
        REPLACE INTO gmair_wechat.message_template (template_id, message_type, message_content, block_flag, create_time)
        VALUES (#{templateId}, #{messageType}, #{messageContent}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="MessageTemplateVo">
        SELECT
        template_id,
        message_type,
        message_content,
        block_flag,
        create_time
        FROM gmair_wechat.message_template
        WHERE 1 = 1
        <if test="templateId != null">
            AND template_id  = #{templateId}
        </if>
        <if test="messageType != null">
            AND message_type = #{messageType}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <update id="update" parameterType="finley.gmair.model.wechat.MessageTemplate">
        UPDATE gmair_wechat.message_template
        SET template_id     = #{templateId},
            message_type    = #{messageType},
            message_content = #{messageContent},
            block_flag      = #{blockFlag},
            create_time     = #{createAt}
        WHERE message_type = #{messageType}
    </update>
</mapper>