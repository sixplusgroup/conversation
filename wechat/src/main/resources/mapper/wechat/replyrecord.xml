<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.wechat.reply.record">
    <resultMap id="replyRecordVo" type="finley.gmair.model.wechat.ReplyRecord">
        <result property="recordId" column="record_id"></result>
        <result property="userId" column="user_id"></result>
        <result property="templateId" column="template_id"></result>
        <result property="input" column="reply_content"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="replyRecordVo">
        SELECT record_id, user_id, template_id, reply_content, block_flag, create_time
        FROM gmair_wechat.reply_record
        WHERE 1 = 1
        <if test="recordId != null">
            AND record_id = #{recordId}
        </if>
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="templateId != null">
            AND template_id = #{templateId}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        <if test="today != null">
            AND date_format(create_time, "%Y-%m-%d") = date_format(now(), "%Y-%m-%d")
        </if>
    </select>

    <insert id="insert" parameterType="finley.gmair.model.wechat.ReplyRecord" useGeneratedKeys="false">
        INSERT INTO gmair_wechat.reply_record (record_id, user_id, template_id, reply_content, block_flag, create_time)
        VALUES (#{recordId}, #{userId}, #{templateId}, #{input}, #{blockFlag}, #{createAt})
    </insert>
</mapper>