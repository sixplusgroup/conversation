<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.wechat.videotemplate">
    <resultMap id="videoTemplateVo" type="finley.gmair.model.wechat.VideoTemplate">
        <result property="templateId" column="template_id"></result>
        <result property="videoUrl" column="video_url"></result>
        <result property="videoTitle" column="video_title"></result>
        <result property="videoDesc" column="video_desc"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.wechat.VideoTemplate" useGeneratedKeys="false">
        INSERT INTO gmair_wechat.video_template(template_id, video_url,video_title,video_desc, block_flag, create_time)
        VALUES (#{templateId}, #{videoUrl},#{videoTitle},#{videoDesc}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="videoTemplateVo">
        SELECT template_id, video_url, video_title,video_desc,block_flag, create_time
        FROM gmair_wechat.video_template
        WHERE 1 = 1
        <if test="templateId != null">
            AND template_id = #{templateId}
        </if>
        <if test="videoUrl != null">
            AND video_url = #{videoUrl}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>