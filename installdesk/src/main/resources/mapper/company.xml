<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.company">
    <resultMap id="companyVo" type="finley.gmair.model.installation.Company">
        <result property="companyId" column="company_id"></result>
        <result property="companyName" column="company_name"></result>
        <result property="messageTitle" column="message_title"></result>
        <result property="companyDetail" column="company_detail"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.installation.Company" useGeneratedKeys="false">
        INSERT INTO gmair_install.company (company_id, company_name, message_title, company_detail,block_flag, create_time)
        VALUES (#{companyId},
        #{companyName},
        #{messageTitle},
        #{companyDetail},
        #{blockFlag},
        #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="companyVo">
        select company_id, company_name, message_title, company_detail,block_flag, create_time
        from gmair_install.company
        where 1 = 1
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        <if test="companyName != null">
            AND company_name = #{companyName}
        </if>
        <if test="messageTitle != null">
            AND message_title = #{messageTitle}
        </if>
        <if test="companyDetail != null">
            AND company_detail = #{companyDetail}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY create_time desc
    </select>
</mapper>