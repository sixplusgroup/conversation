<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.assign.snapshotChangeMachine">
    <resultMap id="snapshotVo" type="finley.gmair.model.installation.SnapshotChangeMachine">
        <result property="snapshotId" column="snapshot_id"></result>
        <result property="assignId" column="assign_id"></result>
        <result property="newCodeValue" column="new_code_value"></result>
        <result property="oldCodeValue" column="old_code_value"></result>
        <result property="picturePath" column="picture_path"></result>
        <result property="wifiConfigured" column="wifi_configured"></result>
        <result property="description" column="description"></result>
        <result property="wayBillNumber" column="waybill_number"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.installation.SnapshotChangeMachine" useGeneratedKeys="false">
        INSERT INTO gmair_install.change_machine_snapshot (snapshot_id, assign_id, new_code_value, old_code_value, picture_path, wifi_configured, description, waybill_number, block_flag, create_time)
        VALUES (#{snapshotId}, #{assignId}, #{newCodeValue}, #{oldCodeValue}, #{picturePath}, #{wifiConfigured},
                #{description}, #{wayBillNumber}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="snapshotVo">
        SELECT snapshot_id, assign_id,  new_code_value, old_code_value, picture_path, wifi_configured, description, waybill_number,
        block_flag, create_time
        FROM gmair_install.change_machine_snapshot
        WHERE 1 = 1
        <if test="snapshotId != null">
            AND snapshot_id = #{snapshotid}
        </if>
        <if test="assignId != null">
            AND assign_id = #{assignId}
        </if>
        <if test="newCodeValue != null">
            AND new_code_value = #{newCodeValue}
        </if>
        <if test="oldCodeValue != null">
            AND old_code_value = #{oldCodeValue}
        </if>
        <if test="picturePath != null">
            AND picture_path = #{picturePath}
        </if>
        <if test="wifiConfigured != null">
            AND wifi_configured = #{wifiConfigured}
        </if>
        <if test="wayBillNumber != null">
            AND waybill_number = #{wayBillNumber}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <update id="block">
        UPDATE gmair_install.change_machine_snapshot
        SET block_flag = TRUE
        WHERE snapshot_id = #{snapshotId}
    </update>

    <delete id="remove">
        DELETE FROM gmair_install.change_machine_snapshot
        WHERE snapshot_id = #{snapshotId}
    </delete>
</mapper>