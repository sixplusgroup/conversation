<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.assign.md5">
    <resultMap id="md5Vo" type="finley.gmair.model.installation.PictureMd5">
        <result property="imageId" column="image_id"></result>
        <result property="picturePath" column="picture_path"></result>
        <result property="md5" column="md5"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.installation.PictureMd5" useGeneratedKeys="false">
        INSERT INTO gmair_install.picture_md5 (image_id, picture_path, md5, block_flag, create_time)
        VALUES (#{imageId}, #{picturePath}, #{md5},#{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="md5Vo">
        SELECT image_id, picture_path, md5, block_flag, create_time
        FROM gmair_install.picture_md5
        WHERE 1 = 1
        <if test="imageId != null">
            AND image_id = #{imageId}
        </if>
        <if test="picturePath != null">
            AND picture_path = #{picturePath}
        </if>
        <if test="md5 != null">
            AND md5 = #{md5}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        <if test="createTime != null">
            AND create_time = #{createTime}
        </if>
    </select>
</mapper>