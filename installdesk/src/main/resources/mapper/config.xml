<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.config">
    <resultMap id="configVo" type="finley.gmair.model.config.GmairConfig">
        <result property="configId" column="config_id"></result>
        <result property="configComp" column="component_name"></result>
        <result property="status" column="component_status"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="createAt"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="configVo">
        SELECT config_id, component_name, component_status, block_flag, create_time
        FROM gmair_install.gmair_config
        WHERE 1 = 1
        <if test="configId != null">
            AND config_id = #{configId}
        </if>
        <if test="configComp != null">
            AND component_name = #{configComp}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>