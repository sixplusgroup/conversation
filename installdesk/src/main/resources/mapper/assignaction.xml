<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.assign.action">
    <resultMap id="actionVo" type="finley.gmair.model.installation.AssignAction">
        <result property="actionId" column="action_id"></result>
        <result property="assignId" column="assign_id"></result>
        <result property="message" column="action_message"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.installation.AssignAction">
        INSERT INTO gmair_install.assign_action (action_id, assign_id, action_message, block_flag, create_time)
        VALUES (#{actionId}, #{assignId}, #{message}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="actionVo">
        SELECT action_id, assign_id, action_message, block_flag, create_time
        FROM gmair_install.assign_action
        WHERE 1 = 1
        <if test="actionId != null">
            AND action_id = #{actionId}
        </if>
        <if test="assignId != null">
            AND assign_id = #{assignId}
        </if>
        <if test="message != null">
            AND action_message = #{message}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY create_time ASC
    </select>
</mapper>