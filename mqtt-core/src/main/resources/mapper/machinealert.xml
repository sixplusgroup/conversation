<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.mqtt.alert">
    <resultMap id="alertVo" type="finley.gmair.model.mqtt.MachineAlert">
        <result property="alertId" column="alert_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="alertCode" column="alert_code"></result>
        <result property="alertMsg" column="alert_msg"></result>
        <result property="alertStatus" column="alert_status"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="alertVo">
        select alert_id, machine_id, alert_code, alert_msg, alert_status, block_flag, create_time
        from gmair_mqtt.machine_alert
        where 1 = 1
        <if test="alertId != null">
            and alert_id = #{alertId}
        </if>
        <if test="machineId != null">
            and machine_id = #{machineId}
        </if>
        <if test="alertCode != null">
            and alert_code = #{alertCode}
        </if>
        <if test="alertMsg != null">
            and alert_msg = #{alertMsg}
        </if>
        <if test="alertStatus != null">
            and alert_status = #{alertStatus}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.mqtt.MachineAlert" useGeneratedKeys="false">
        insert into gmair_mqtt.machine_alert(alert_id, machine_id, alert_code, alert_msg, alert_status, block_flag, create_time)
        values(#{alertId}, #{machineId}, #{alertCode}, #{alertMsg}, #{alertStatus}, #{blockFlag}, #{createAt})
    </insert>

    <update id="update" parameterType="java.util.Map">
        update gmair_mqtt.machine_alert
        set alert_status = #{alertStatus}
        where alert_id = #{alertId}
    </update>

    <update id="updateBatch" parameterType="java.util.Map">
        update gmair_mqtt.machine_alert
        set alert_status = #{alertStatus}
        where alert_id in
        <foreach collection="list" index="index" item="MachineAlert" open="(" separator="," close=")">
            #{MachineAlert.alertId}
        </foreach>
    </update>
</mapper>