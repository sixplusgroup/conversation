<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.mqtt.machineType">
    <resultMap id="typeVo" type="finley.gmair.model.mqtt.MachineType">
        <result property="boardId" column="board_id"></result>
        <result property="boardVersion" column="board_version"></result>
        <result property="deviceName" column="device_name"></result>
        <result property="typeName" column="type_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="typeVo">
        select board_id, board_version, device_name, type_name, block_flag, create_time
        from gmair_mqtt.machine_type
        where 1 = 1
        <if test="boardId != null">
            and board_id = #{boardId}
        </if>
        <if test="boardVersion != null">
            and board_version = #{boardVersion}
        </if>
        <if test="deviceName != null">
            and device_name = #{deviceName}
        </if>
        <if test="typeName != null">
            and type_name = #{typeName}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.mqtt.MachineType" useGeneratedKeys="false">
        insert into gmair_mqtt.machine_type(board_id, board_version, device_name, type_name, block_flag, create_time)
        values(#{boardId}, #{boardVersion}, #{deviceName}, #{typeName}, #{blockFlag}, #{createAt})
    </insert>
</mapper>