<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.mqtt.apibound">
    <resultMap id="boundVo" type="finley.gmair.model.mqtt.ApiBound">
        <result property="boundId" column="bound_id"></result>
        <result property="apiName" column="api_name"></result>
        <result property="apiUrl" column="api_url"></result>
        <result property="apiTopic" column="api_topic"></result>
        <result property="apiDescription" column="api_description"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="boundVo">
        select bound_id, api_name, api_url, api_topic, api_description, block_flag, create_time
        from gmair_mqtt.api_bound
        where 1 = 1
        <if test="boundId != null">
            and bound_id = #{boundId}
        </if>
        <if test="apiName != null">
            and api_name = #{apiName}
        </if>
        <if test="apiUrl != null">
            and api_url = #{apiUrl}
        </if>
        <if test="apiTopic != null">
            and api_topic = #{apiTopic}
        </if>
        <if test="apiDescription != null">
            and api_description = #{apiDescription}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.mqtt.ApiBound">
        insert into gmair_mqtt.api_bound(bound_id, api_name, api_url, api_topic, api_description, block_flag, create_time)
        values (#{boundId}, #{apiName}, #{apiUrl}, #{apiTopic}, #{apiDescription}, #{blockFlag}, #{createAt})
    </insert>

    <update id="update" parameterType="java.util.Map">
        update gmair_mqtt.api_bound
        <set>
            <if test="apiName != null">
                api_name = #{apiName},
            </if>
            <if test="apiUrl != null">
                api_url = #{apiUrl},
            </if>
            <if test="apiTopic != null">
                api_topic = #{apiTopic},
            </if>
            <if test="apiDescription != null">
                api_description = #{apiDescription},
            </if>
            <if test="blockFlag != null">
                block_flag = #{blockFlag},
            </if>
            bound_id = #{boundId}
        </set>
        where bound_id = #{boundId}
    </update>
</mapper>