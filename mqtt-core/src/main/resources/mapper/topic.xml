<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.mqtt.topic">
    <resultMap id="topicVo" type="finley.gmair.model.mqtt.Topic">
        <result property="topicId" column="topic_id"></result>
        <result property="topicDetail" column="topic_detail"></result>
        <result property="topicDescription" column="topic_description"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="topicVo">
        select topic_id, topic_detail, topic_description, block_flag, create_time
        from gmair_mqtt.mqtt_topic
        where 1 = 1
        <if test="topicId != null">
            and topic_id = #{topicId}
        </if>
        <if test="topicDetail != null">
            and topic_detail = #{topicDetail}
        </if>
        <if test="topicDescription != null">
            and topic_description = #{topicDescription}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.mqtt.Topic" useGeneratedKeys="false">
        insert into gmair_mqtt.mqtt_topic(topic_id, topic_detail, topic_description, block_flag, create_time)
        values(#{topicId}, #{topicDetail}, #{topicDescription}, #{blockFlag}, #{createAt})
    </insert>

    <update id="update" parameterType="java.util.Map">
        update gmair_mqtt.mqtt_topic
        <set>
            <if test="topicDetail != null">
                topic_detail = #{topicDetail},
            </if>
            <if test="topicDescription != null">
                topic_description = #{topicDescription},
            </if>
            topic_id = #{topicId}
        </set>
        where topic_id = #{topicId}
    </update>
</mapper>