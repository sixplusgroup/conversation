<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.express.order">
    <resultMap id="orderVo" type="finley.gmair.model.express.ExpressOrder">
        <result property="expressId" column="express_id"></result>
        <result property="orderId" column="order_id"></result>
        <result property="companyId" column="company_id"></result>
        <result property="expressNo" column="express_no"></result>
        <result property="expressStatus" column="express_status"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.express.ExpressOrder" useGeneratedKeys="false">
        INSERT INTO gmair_express.order_express(express_id, order_id, company_id, express_no, express_status, block_flag, create_time)
        VALUES (#{expressId}, #{orderId}, #{companyId}, #{expressNo}, #{expressStatus}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="orderVo">
        SELECT express_id, order_id, company_id, express_no, express_status, block_flag, create_time
        FROM gmair_express.order_express
        WHERE 1 = 1
        <if test="expressId != null">
            AND express_id = #{expressId}
        </if>
        <if test="order_id != null">
            AND order_id = #{orderId}
        </if>
        <if test="company_id != null">
            AND company_id= #{companyId}
        </if>
        <if test="express_no != null">
            AND express_no= #{expressNo}
        </if>
        <if test="express_status != null">
            AND express_status= #{expressStatus}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>