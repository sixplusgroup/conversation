<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.humid_daily">
    <resultMap id="humidDaily" type="finley.gmair.model.dataAnalysis.HumidHourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="averageHumid" column="average_humid"></result>
        <result property="maxHumid" column="max_humid"></result>
        <result property="minHumid" column="min_humid"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.dataAnalysis.HumidHourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.humid_daily(status_id, machine_id, average_humid, max_humid, min_humid, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{averageHumid}, #{maxHumid}, #{minHumid}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.humid_daily(status_id, machine_id, average_humid, max_humid, min_humid, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="HumidDaily" index="index">
            (#{HumidDaily.statusId}, #{HumidDaily.machineId},
            #{HumidDaily.averageHumid}, #{HumidDaily.maxHumid}, #{HumidDaily.minHumid},
            #{HumidDaily.blockFlag}, #{HumidDaily.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="humidDaily">
        SELECT status_id, machine_id, average_humid, max_humid, min_humid, block_flag, create_time
        FROM gmair_data_analysis.humid_daily
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>