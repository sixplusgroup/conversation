<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.mode_hourly">
    <resultMap id="modeHourly" type="finley.gmair.model.dataAnalysis.ModeHourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="manualMinute" column="manual_minute"></result>
        <result property="cosyMinute" column="cosy_minute"></result>
        <result property="warmMinute" column="warm_minute"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.dataAnalysis.ModeHourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.mode_hourly(status_id, machine_id, manual_minute, cosy_minute, warm_minute, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{manualMinute}, #{cosyMinute}, #{warmMinute}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.mode_hourly(status_id, machine_id, manual_minute, cosy_minute, warm_minute, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="ModeHourly" index="index">
            (#{ModeHourly.statusId}, #{ModeHourly.machineId},
            #{ModeHourly.manualMinute}, #{ModeHourly.cosyMinute}, #{ModeHourly.warmMinute},
            #{ModeHourly.blockFlag}, #{ModeHourly.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="modeHourly">
        SELECT status_id, machine_id, manual_minute, cosy_minute, warm_minute, block_flag, create_time
        FROM gmair_data_analysis.mode_hourly
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>