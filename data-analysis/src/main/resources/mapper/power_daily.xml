<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.power_daily">
    <resultMap id="powerDaily" type="finley.gmair.model.dataAnalysis.PowerHourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="powerOnMinute" column="power_on_minute"></result>
        <result property="powerOffMinute" column="power_off_minute"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.dataAnalysis.PowerHourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.power_daily(status_id, machine_id, power_on_minute, power_off_minute, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{powerOnMinute}, #{powerOffMinute}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.power_daily(status_id, machine_id, power_on_minute, power_off_minute, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="PowerDaily" index="index">
            (#{PowerDaily.statusId}, #{PowerDaily.machineId},
            #{PowerDaily.powerOnMinute}, #{PowerDaily.powerOffMinute},
            #{PowerDaily.blockFlag}, #{PowerDaily.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="powerDaily">
        SELECT status_id, machine_id, power_on_minute, power_off_minute, block_flag, create_time
        FROM gmair_data_analysis.power_daily
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>