<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.indoor_pm25_hourly">
    <resultMap id="indoorPm25Hourly" type="finley.gmair.model.dataAnalysis.IndoorPm25Hourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="averagePm25" column="average_pm25"></result>
        <result property="maxPm25" column="max_pm25"></result>
        <result property="minPm25" column="min_pm25"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.dataAnalysis.IndoorPm25Hourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.indoor_pm25_hourly(status_id, machine_id, average_pm25, max_pm25, min_pm25, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{averagePm25}, #{maxPm25}, #{minPm25}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.indoor_pm25_hourly(status_id, machine_id, average_pm25, max_pm25, min_pm25, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="IndoorPm25Hourly" index="index">
            (#{IndoorPm25Hourly.statusId}, #{IndoorPm25Hourly.machineId},
            #{IndoorPm25Hourly.averagePm25}, #{IndoorPm25Hourly.maxPm25}, #{IndoorPm25Hourly.minPm25},
            #{IndoorPm25Hourly.blockFlag}, #{IndoorPm25Hourly.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="indoorPm25Hourly">
        SELECT status_id, machine_id, power_usage, block_flag, create_time
        FROM gmair_data_analysis.indoor_pm25_hourly
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>