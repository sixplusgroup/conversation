<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.temp_hourly">
    <resultMap id="tempHourly" type="finley.gmair.model.dataAnalysis.TempHourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="averageTemp" column="average_temp"></result>
        <result property="maxTemp" column="max_temp"></result>
        <result property="minTemp" column="min_temp"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.dataAnalysis.TempHourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.temp_hourly(status_id, machine_id, average_temp, max_temp, min_temp, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{averageTemp}, #{maxTemp}, #{minTemp}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.temp_hourly(status_id, machine_id, average_temp, max_temp, min_temp, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="TempHourly" index="index">
            (#{TempHourly.statusId}, #{TempHourly.machineId},
            #{TempHourly.averageTemp}, #{TempHourly.maxTemp}, #{TempHourly.minTemp},
            #{TempHourly.blockFlag}, #{TempHourly.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="tempHourly">
        SELECT status_id, machine_id, average_temp, max_temp, min_temp, block_flag, create_time
        FROM gmair_data_analysis.temp_hourly
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>