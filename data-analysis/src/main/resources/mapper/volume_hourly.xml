<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.volume_hourly">
    <resultMap id="volumeHourly" type="finley.gmair.model.analysis.VolumeHourly">
        <result property="statusId" column="status_id"></result>
        <result property="machineId" column="machine_id"></result>
        <result property="averageVolume" column="average_volume"></result>
        <result property="maxVolume" column="max_volume"></result>
        <result property="minVolume" column="min_volume"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.analysis.VolumeHourly" useGeneratedKeys="false">
        INSERT INTO gmair_data_analysis.volume_hourly(status_id, machine_id, average_volume, max_volume, min_volume, block_flag, create_time)
        VALUES(#{statusId}, #{machineId}, #{averageVolume}, #{maxVolume}, #{minVolume}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO gmair_data_analysis.volume_hourly(status_id, machine_id, average_volume, max_volume, min_volume, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="VolumeHourly" index="index">
            (#{VolumeHourly.statusId}, #{VolumeHourly.machineId},
            #{VolumeHourly.averageVolume}, #{VolumeHourly.maxVolume}, #{VolumeHourly.minVolume},
            #{VolumeHourly.blockFlag}, #{VolumeHourly.createAt})
        </foreach>
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="volumeHourly">
        SELECT status_id, machine_id, average_volume, max_volume, min_volume, block_flag, create_time
        FROM gmair_data_analysis.volume_hourly
        WHERE 1 = 1
        <if test="statusId != null">
            AND status_id = #{statusId}
        </if>
        <if test="machineId != null">
            AND machine_id = #{machineId}
        </if>
        <if test="createTimeGT != null">
            AND create_time > #{createTimeGT}
        </if>
        <if test="createTimeGTE != null">
            AND create_time >= #{createTimeGTE}
        </if>
        <if test="createTimeLT != null">
            <![CDATA[
                AND create_time < #{createTimeLT}
            ]]>
        </if>
        <if test="createTimeLTE != null">
            <![CDATA[
                AND create_time <= #{createTimeLTE}
            ]]>
        </if>
        order by create_time asc
    </select>
</mapper>