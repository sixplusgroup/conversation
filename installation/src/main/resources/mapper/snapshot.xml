<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.setupprovider">
    <resultMap id="Snapshot" type="finley.gmair.model.installation.Snapshot">
        <result property="providerId" column="provider_id" />
        <result property="providerName" column="provider_name" />
        <result property="blockFlag" column="block_flag"/>
        <result property="createTime" column="create_time" />
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="SetupProviderVo">
        SELECT *
        FROM `gmair_order`.`setup_provider`
        where 1 = 1
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>