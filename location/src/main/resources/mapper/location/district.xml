<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.location.district">
    <resultMap id="districtVo" type="finley.gmair.model.district.District">
        <result property="districtId" column="district_id"></result>
        <result property="districtName" column="district_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <resultMap id="districtCityVo" type="finley.gmair.vo.location.DistrictCityVo">
        <result property="districtId" column="district_id"></result>
        <result property="cityId" column="city_id"></result>
        <result property="districtName" column="district_name"></result>
    </resultMap>

    <insert id="insert" parameterType="java.util.Map" useGeneratedKeys="false">
        INSERT INTO gmair_location.district_list (district_id, city_id, district_name, block_flag, create_time)
        VALUES
            (#{district.districtId}, #{cityId}, #{district.districtName}, #{district.blockFlag}, #{district.createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="districtVo">
        SELECT district_id, district_name, block_flag, create_time
        FROM gmair_location.district_list
        WHERE 1 = 1
        <if test="districtId != null">
            AND district_id = #{districtId}
        </if>
        <if test="cityId != null">
            AND city_id = #{cityId}
        </if>
        <if test="districtName != null">
            AND district_name = #{districtName}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <select id="queryCity" parameterType="java.util.Map" resultMap="districtCityVo">
        SELECT district_id, city_id, district_name
        FROM gmair_location.district_list
        WHERE 1 = 1
        <if test="districtId != null">
            AND district_id = #{districtId}
        </if>
    </select>

    <update id="update" parameterType="finley.gmair.model.district.District">
        UPDATE gmair_location.district_list
        SET
            district_name = #{districtName},
            block_flag    = #{blockFlag}
        WHERE district_id = #{districtId}
    </update>
</mapper>