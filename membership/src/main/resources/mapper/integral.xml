<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.membership.integral">
    <insert id="insert" parameterType="finley.gmair.model.membership.IntegralAdd">
        INSERT INTO gmair_membership.integral_add (add_id, consumer_id, integral_value, is_confirmed, create_time)
            VALUE (#{addId}, #{consumerId}, #{integralValue}, #{isConfirmed}, #{createAt})
    </insert>
    <select id="selectOne" resultType="finley.gmair.model.membership.IntegralAdd">
        SELECT *
        FROM gmair_membership.integral_add
        WHERE add_id = #{addId}
    </select>
    <update id="updateConfirm">
        UPDATE gmair_membership.integral_add
        SET is_confirmed = TRUE, confirmed_time = now()
        WHERE add_id = #{addId}
    </update>
</mapper>