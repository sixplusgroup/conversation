<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.enterprise_selling.merchant_profile">
    <resultMap id="merchantProfile" type="finley.gmair.model.enterpriseselling.MerchantProfile">
        <result property="merchantId" column="merchant_id"></result>
        <result property="merchantName" column="merchant_name"></result>
        <result property="merchantCode" column="merchant_code"></result>
        <result property="merchantAddress" column="merchant_address"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.enterpriseselling.MerchantProfile" useGeneratedKeys="false">
        INSERT INTO gmair_enterprise.merchant_profile(merchant_id, merchant_name, merchant_code, merchant_address, block_flag, create_time)
        VALUES(#{merchantId}, #{merchantName}, #{merchantCode}, #{merchantAddress}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="merchantProfile">
        SELECT merchant_id,merchant_name,merchant_code,merchant_address,block_flag,create_time
        FROM gmair_enterprise.merchant_profile
        WHERE 1 = 1
        <if test="merchantId != null">
            AND merchant_id = #{merchantId}
        </if>
        <if test="merchantName != null">
            AND merchant_name = #{merchantName}
        </if>
        <if test="merchantCode != null">
            AND merchant_code = #{merchantCode}
        </if>
        <if test="merchantAddress != null">
            AND merchant_address = #{merchantAddress}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <update id="update" parameterType="java.util.Map">
        UPDATE gmair_enterprise.merchant_profile
        <set>
            <if test="merchantId != null">
                merchant_id = #{merchantId},
            </if>
            <if test="merchantName != null">
                merchant_name = #{merchantName},
            </if>
            <if test="merchantCode != null">
                merchant_code = #{merchantCode},
            </if>
            <if test="merchantAddress != null">
                merchant_address = #{merchantAddress},
            </if>
        merchant_id = #{merchant_id}
        </set>
        WHERE merchant_id = #{merchant_id}
    </update>
</mapper>