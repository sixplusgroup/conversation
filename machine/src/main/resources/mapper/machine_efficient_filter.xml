<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.machine_efficient_filter">
    <resultMap id="machine_efficient_filter" type="finley.gmair.model.machine.MachineEfficientFilter">
        <result property="qrcode" column="qr_code"></result>
        <result property="replaceStatus" column="replace_status"
                typeHandler="finley.gmair.mybatis.handler.EfficientFilterStatusHandler"></result>
        <result property="replaceRemindOn" column="replace_remind_on"></result>
        <result property="isRemindedStatus" column="is_reminded_status"
                typeHandler="finley.gmair.mybatis.handler.EfficientFilterRemindStatusHandler"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="machine_efficient_filter">
        SELECT qr_code, replace_status, replace_remind_on, is_reminded_status, block_flag, create_time
        FROM gmair_machine.machine_efficient_filter
        WHERE 1 = 1
        <if test="qrcode != null">
            AND qr_code = #{qrcode}
        </if>
        <if test="replaceStatus!= null">
            AND replace_status = #{replaceStatus}
        </if>
        <if test="replaceRemindOn != null">
            AND replace_remind_on = #{replaceRemindOn}
        </if>
        <if test="isRemindedStatus != null">
            AND is_reminded_status = #{isRemindedStatus}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <select id="query_need_remind_first" resultMap="machine_efficient_filter">
        SELECT qr_code, replace_status, replace_remind_on, is_reminded_status, block_flag, create_time
        FROM gmair_machine.machine_efficient_filter
        WHERE block_flag = 0
        AND replace_status = 1
        AND replace_remind_on = true
        AND is_reminded_status = 0
    </select>

    <select id="query_need_remind_second" resultMap="machine_efficient_filter">
        SELECT qr_code, replace_status, replace_remind_on, is_reminded_status, block_flag, create_time
        FROM gmair_machine.machine_efficient_filter
        WHERE block_flag = 0
        AND replace_status = 2
        AND replace_remind_on = true
        AND is_reminded_status &lt;&gt; 2
    </select>

    <update id="update" parameterType="java.util.Map">
        UPDATE gmair_machine.machine_efficient_filter
        <set>
            <if test="replaceStatus!= null">
                replace_status = #{replaceStatus},
            </if>
            <if test="replaceRemindOn != null">
                replace_remind_on = #{replaceRemindOn},
            </if>
            <if test="isRemindedStatus != null">
                is_reminded_status = #{isRemindedStatus},
            </if>
            <if test="blockFlag != null">
                block_flag = #{blockFlag},
            </if>
            qr_code = #{qrcode}
        </set>
        WHERE qr_code = #{qrcode}
    </update>

    <insert id="insert" parameterType="finley.gmair.model.machine.MachineEfficientFilter" useGeneratedKeys="false">
        INSERT INTO gmair_machine.machine_efficient_filter(qr_code, replace_status, replace_remind_on, is_reminded_status, block_flag, create_time)
        VALUES(#{qrcode}, #{replaceStatus,typeHandler=finley.gmair.mybatis.handler.EfficientFilterStatusHandler},
         #{replaceRemindOn}, #{isRemindedStatus,typeHandler=finley.gmair.mybatis.handler.EfficientFilterRemindStatusHandler},
          #{blockFlag}, #{createAt})
    </insert>

</mapper>