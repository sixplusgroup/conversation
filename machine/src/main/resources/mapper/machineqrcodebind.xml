<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.qrcode.bind">
    <resultMap id="screenRecordVo" type="finley.gmair.model.machine.v2.ScreenRecord">
        <result property="recordId" column="record_id"></result>
        <result property="codeValue" column="code_value"></result>
        <result property="refreshDate" column="refresh_date"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="screenRecordVo">
        SELECT record_id, code_value, refresh_date, block_flag, create_time
        FROM gmair_machine.screen_record
        WHERE 1 = 1
        <if test="recordId != null">
            AND record_id = #{recordId}
        </if>
        <if test="codeValue != null">
            AND code_value = #{codeValue}
        </if>
        <if test="refreshDate != null">
            AND refresh_date = #{refreshDate}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="finley.gmair.model.machine.MachineQrcodeBind" useGeneratedKeys="false">
        INSERT INTO `gmair_machine`.`code_machine_bind` (bind_id, code_value, machine_id, block_flag, create_time)
        VALUES(#{bindId}, #{codeValue}, #{machineId}, #{blockFlag}, #{createAt})
    </insert>
</mapper>