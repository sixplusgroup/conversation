<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.airquality.machineairquality">

    <insert id="insert" parameterType="finley.gmair.model.air.MachineAirQuality">
        INSERT INTO `gmair_airquality`.`machine_airquality`
        (ma_id, codeValue, pm_25, temperature, humidity, co2, block_flag, create_time)
        VALUES
        (#{maId}, #{codeValue}, #{pm25}, #{temperature}, #{humidity}, #{co2}, #{blockFlag}, #{createAt})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO `gmair_airquality`.`machine_airquality`
        (ma_id, codeValue, pm_25, temperature, humidity, co2, block_flag, create_time)
        VALUES
        <foreach collection="list" separator="," item="airQuality" index="index">
            (#{airQuality.maId}, #{airQuality.codeValue}, #{airQuality.pm25m}, #{airQuality.temperature},
             #{airQuality.humidity}, #{airQuality.co2}, #{airQuality.blockFlag}, #{airQuality.createAt}
            )
        </foreach>
    </insert>

</mapper>