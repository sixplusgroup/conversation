<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.text">
    <resultMap id="text" type="finley.gmair.model.machine.Text">
        <result property="textId" column="text_id"></result>
        <result property="textType" column="text_type"></result>
        <result property="textContent" column="text_content"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="text">
        SELECT text_id, text_type, text_content, block_flag, create_time
        FROM gmair_machine.text
        WHERE 1 = 1
        <if test="textId != null">
            AND text_id = #{textId}
        </if>
        <if test="textType != null">
            AND text_type = #{textType}
        </if>
        <if test="textContent != null">
            AND text_content = #{textContent}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>