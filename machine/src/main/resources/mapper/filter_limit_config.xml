<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.filter.limit.config">
    <resultMap id="configVo" type="finley.gmair.model.machine.FilterLimitConfig">
        <result property="configId" column="config_id"></result>
        <result property="modelId" column="model_id"></result>
        <result property="overCountLimit" column="over_count_limit"></result>
        <result property="overPm25Limit" column="over_pm25_limit"></result>
        <result property="duration" column="duration"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="configVo">
        SELECT
        config_id,
        model_id,
        over_count_limit,
        over_pm25_limit,
        duration,
        block_flag,
        create_time
        FROM gmair_machine.filter_limit_config
        WHERE 1 = 1
        <if test="configId != null">
            AND config_id = #{configId}
        </if>
        <if test="modelId != null">
            AND model_id = #{modelId}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>