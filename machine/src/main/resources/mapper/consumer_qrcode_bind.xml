<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.consumer_qrcode_bind">
    <resultMap id="consumer_qrcode_bind" type="finley.gmair.model.machine.ConsumerQRcodeBind">
        <result property="bindId" column="bind_id"></result>
        <result property="consumerId" column="consumer_id"></result>
        <result property="bindName" column="bind_name"></result>
        <result property="codeValue" column="code_value"></result>
        <result property="ownership" column="ownership"
                typeHandler="finley.gmair.mybatis.handler.OwnershipHandler"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.machine.ConsumerQRcodeBind" useGeneratedKeys="false">
        INSERT INTO gmair_machine.consumer_code_bind(bind_id, consumer_id, bind_name, code_value, ownership, block_flag, create_time)
        VALUES (#{bindId}, #{consumerId}, #{bindName}, #{codeValue},
        #{ownership,typeHandler=org.apache.ibatis.type.EnumOrdinalTypeHandler},
        #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map">
        SELECT bind_id, consumer_id, bind_name, code_value, ownership, block_flag, create_time
        FROM gmair_machine.consumer_code_bind
        WHERE 1 = 1
        <if test="bindId != null">
            AND bind_id = #{bindId}
        </if>
        <if test="consumerId != null">
            AND consumer_id = #{consumerId}
        </if>
        <if test="codeValue != null">
            AND code_value = #{codeValue}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>

    </select>
</mapper>