<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.machine.qrcode">
    <resultMap id="qrcodeVo" type="finley.gmair.model.machine.QRCode">
        <result property="codeId" column="code_id"></result>
        <result property="modelId" column="model_id"></result>
        <result property="batchValue" column="batch_value"></result>
        <result property="codeValue" column="code_value"></result>
        <result property="codeUrl" column="code_url"></result>
        <result property="status" column="code_status"
                typeHandler="finley.gmair.mybatis.handler.QRCodeStatusHandler"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <resultMap id="batchVo" type="finley.gmair.vo.machine.BatchVo">
        <result property="batchValue" column="batch_value"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="qrcodeVo">
        SELECT code_id, model_id, batch_value, code_value, code_url, code_status, block_flag, create_time
        FROM gmair_machine.qrcode
        WHERE 1 = 1
        <if test="codeId != null">
            AND code_id = #{codeId}
        </if>
        <if test="modelId != null">
            AND model_id = #{modelId}
        </if>
        <if test="batchValue != null">
            AND batch_value = #{batchValue}
        </if>
        <if test="codeValue != null">
            AND code_value = #{codeValue}
        </if>
        <if test="codeUrl != null">
            AND code_url = #{codeUrl}
        </if>
        <if test="codeStatus">
            AND code_status = #{status}
        </if>
        <if test="search != null">
            and (model_id like #{search} or code_value like #{search} or batch_value like #{search})
        </if>
        ORDER BY batch_value ASC, code_value ASC
    </select>

    <insert id="insert" parameterType="finley.gmair.model.machine.QRCode" useGeneratedKeys="false">
        INSERT INTO gmair_machine.qrcode(code_id, model_id, batch_value, code_value, code_url, code_status, block_flag, create_time)
        VALUES(#{codeId}, #{modelId}, #{batchValue}, #{codeValue}, #{codeUrl}, #{status, typeHandler=org.apache.ibatis.type.EnumOrdinalTypeHandler}, #{blockFlag}, #{createAt})
    </insert>

    <select id="batchQuery" parameterType="java.util.Map" resultMap="batchVo">
        SELECT DISTINCT batch_value 
        FROM gmair_machine.qrcode
        WHERE 1 = 1
        <if test="modelId != null">
            AND model_id = #{modelId}
        </if>
    </select>
</mapper>