<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="finley.gmair.dao.JdUserDOMapper">
    <select id="getUserIdByName" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT usr_id as id from usr_jd where usr_name=#{name}
        <if test="id==null">
            insert into usr_jd set usr_name=#{name}
            SELECT LAST_INSERT_ID()
        </if>
    </select>

</mapper>