<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="finley.gmair.dao.SessionMessageDOMapper">
    <insert id="insertSessionMessage" parameterType="java.lang.Integer">
        insert into session_message_jd(session_id, content, is_from_waiter, timestamp) values
        (#{sid}, #{content}, #{isFromWaiter}, #{timestamp})
        <selectKey keyProperty="message_id" order="AFTER" resultType="java.lang.Integer">
            SELECT message_id from session_message_jd
        </selectKey>
    </insert>

</mapper>