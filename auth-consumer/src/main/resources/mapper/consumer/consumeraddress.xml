<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.consumer.address">
    <resultMap id="addressVo" type="finley.gmair.model.consumer.Address">
        <result property="addressId" column="addr_id"></result>
        <result property="detail" column="address_detail"></result>
        <result property="province" column="address_province"></result>
        <result property="city" column="address_city"></result>
        <result property="district" column="address_district"></result>
        <result property="preferred" column="addr_preferred"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="addressVo">
        SELECT addr_id, address_detail, address_province, address_city, address_district, addr_preferred, block_flag, create_time
        FROM gmair_userinfo.consumer_addr
        WHERE 1 = 1
        <if test="addressId != null">
            AND addr_id = #{addressId}
        </if>
        <if test="consumerId != null">
            AND consumer_id = #{consumerId}
        </if>
        <if test="preferred != null">
            AND addr_preferred = #{preferred}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>

    <insert id="insert" parameterType="java.util.Map" useGeneratedKeys="false">
        INSERT INTO gmair_userinfo.consumer_addr(addr_id, consumer_id, address_detail, address_province, address_city, address_district, addr_preferred, block_flag, create_time)
        VALUES (#{address.addressId}, #{consumerId}, #{address.detail}, #{address.province}, #{address.city}, #{address.district}, #{address.preferred}, #{address.blockFlag}, #{address.createAt})
    </insert>

    <update id="update" parameterType="java.util.Map">
        UPDATE gmair_userinfo.consumer_addr
        <set>
            <if test="detail != null">
                address_detail = #{detail},
            </if>
            <if test="province != null">
                address_province = #{province},
            </if>
            <if test="city != null">
                address_city = #{city},
            </if>
            <if test="district != null">
                address_district = #{district},
            </if>
            <if test="preferred != null">
                addr_preferred = #{preferred},
            </if>
            <if test="blockFlag != null">
                block_flag = #{blockFlag},
            </if>
            addr_id = #{addressId}
        </set>
        WHERE addr_id = #{addressId}
    </update>
</mapper>