<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.channel">
    <resultMap id="channelVo" type="finley.gmair.model.drift.DriftChannel">
        <result property="channelId" column="channel_id"></result>
        <result property="channelName" column="channel_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.drift.DriftChannel">
        insert into gmair_drift.drift_channel(channel_id, channel_name, block_flag, create_time)
        values(#{channelId}, #{channelName}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="channelVo">
        select channel_id, channel_name, block_flag, create_time
        from gmair_drift.drift_channel
        where 1 = 1
        <if test="channelId != null">
            and channel_id = #{channelId}
        </if>
        <if test="channelName != null">
            and channel_name = #{channelName}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>
</mapper>