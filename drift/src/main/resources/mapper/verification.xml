<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.verification">
    <resultMap id="verifyInfo" type="finley.gmair.model.drift.VerifyInfo">
        <result property="verifyId" column="verify_id"></result>
        <result property="openid" column="openid"></result>
        <result property="idCard" column="id_card"></result>
        <result property="name" column="id_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="verifyInfo">
        select verify_id, openid, id_card, id_name, block_flag, create_time
        from gmair_drift.drift_verification
        where 1 = 1
        <if test="openid != null">
            AND openid = #{openid}
        </if>
        <if test="idCard != null">
            AND id_card = #{idCard}
        </if>
        <if test="name != null">
            AND id_name = #{name}
        </if>
    </select>

    <insert id="insert" parameterType="finley.gmair.model.drift.VerifyInfo" useGeneratedKeys="false">
        INSERT INTO gmair_drift.drift_verification (verify_id, openid, id_card, id_name, block_flag, create_time)
        VALUES (#{verifyId}, #{openid}, #{idCard}, #{name}, #{blockFlag}, #{createAt})
    </insert>
</mapper>