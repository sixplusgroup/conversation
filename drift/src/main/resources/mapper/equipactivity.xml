<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.equip_activity">
    <resultMap id="relationVo" type="finley.gmair.model.drift.EquipActivity">
        <result property="equipId" column="equip_id"></result>
        <result property="activityId" column="activity_id"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="relationVo">
        select equip_id, activity_id, block_flag, create_time
        from gmair_drift.equip_activity
        where 1 = 1
        <if test="equipId != null">
            and equip_id = #{equipId}
        </if>
        <if test="activityId != null">
            and activity_id = #{activityId}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        order by create_time desc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.drift.EquipActivity">
        insert into gmair_drift.equip_activity(equip_id, activity_id, block_flag, create_time)
        values(#{equipId}, #{activityId}, #{blockFlag}, #{createAt})
    </insert>
</mapper>