<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.promotion">
    <resultMap id="promotionVo" type="finley.gmair.model.drift.DriftPromotion">
        <result property="promotionid" column="promotion_id"></result>
        <result property="activityid" column="activity_id"></result>
        <result property="description" column="promotion_description"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" resultMap="promotionVo">
        SELECT promotion_id, activity_id, promotion_description, block_flag, create_time
        FROM gmair_drift.drift_promotion
        WHERE 1 = 1
        <if test="promotionId != null">
            AND promotion_id = #{promotionId}
        </if>
        <if test="activityId != null">
            AND activity_id = #{activityId}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>