<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.activity.thumbnail">
    <resultMap id="thumbnailVo" type="finley.gmair.model.drift.Thumbnail">
        <result property="thumbnailId" column="thumbnail_id"></result>
        <result property="thumbnailPath" column="thumbnail_path"></result>
        <result property="thumbnailIndex" column="thumbnail_index"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" resultMap="thumbnailVo">
        SELECT thumbnail_id, thumbnail_path, thumbnail_index, block_flag, create_time
        FROM gmair_drift.activity_thumbnail
        WHERE 1 = 1
        <if test="thumbnailId != null">
            AND thumbnail_id = #{thumbnailId}
        </if>
        <if test="activityId != null">
            AND activity_id = #{activityId}
        </if>
        <if test="thumbnailIndex != null">
            AND thumbnail_index = #{thumbnailIndex}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
        ORDER BY thumbnail_index ASC
    </select>

    <insert id="insert" parameterType="finley.gmair.model.drift.Thumbnail" useGeneratedKeys="false">
        INSERT INTO gmair_drift.activity_thumbnail (thumbnail_id, thumbnail_path, thumbnail_index, block_flag, create_time)
        VALUES (#{thumbnailId}, #{thumbnailPath}, #{thumbnailIndex}, #{blockFlag}, #{createAt})
    </insert>
</mapper>