<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.drift.reporttemplate">
    <resultMap id="templateVo" type="finley.gmair.model.drift.ReportTemplate">
        <result property="reportTemplateId" column="report_template_id"></result>
        <result property="detectName" column="detect_name"></result>
        <result property="evaluateBasis" column="evaluate_basis"></result>
        <result property="knowledge" column="knowledge"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="templateVo">
        select report_template_id, detect_name, evaluate_basis, knowledge, block_flag, create_time
        from gmair_drift.report_template
        where 1 = 1
        <if test="reportTemplateId != null">
            AND report_template_id = #{reportTemplateId}
        </if>
        <if test="detectName != null">
            AND detect_name = #{detectName}
        </if>
        <if test="evaluateBasis != null">
            AND evaluate_basis = #{evaluateBasis}
        </if>
        <if test="knowledge != null">
            AND knowledge = #{knowledge}
        </if>
        <if test="blockFlag != null">
            and block_flag = #{blockFlag}
        </if>
        ORDER BY create_time asc
    </select>

    <insert id="insert" parameterType="finley.gmair.model.drift.ReportTemplate" useGeneratedKeys="false">
        insert into gmair_drift.report_template (report_template_id, detect_name, evaluate_basis, knowledge, block_flag, create_time)
        VALUES (#{reportTemplateId},
        #{detectName},
        #{evaluateBasis},
        #{knowledge},
        #{blockFlag},
        #{createAt})
    </insert>
</mapper>