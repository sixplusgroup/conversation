<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.formaldehyde.case_lng_lat">
    <resultMap id="caseLngLat" type="finley.gmair.model.formaldehyde.CaseLngLat">
        <result property="recordId" column="record_id"></result>
        <result property="caseId" column="case_id"></result>
        <result property="longitude" column="longitude"></result>
        <result property="latitude" column="latitude"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.formaldehyde.CaseLngLat" useGeneratedKeys="false">
        INSERT INTO gmair_formaldehyde_detection.case_lng_lat(record_id, case_id, longitude, latitude, block_flag, create_time)
        VALUES(#{recordId}, #{caseId}, #{longitude}, #{latitude}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="caseLngLat">
        SELECT record_id,case_id,longitude,latitude,block_flag,create_time
        FROM gmair_formaldehyde_detection.case_lng_lat
        WHERE 1 = 1
        <if test="recordId != null">
            AND record_id = #{recordId}
        </if>
        <if test="caseId != null">
            AND case_id = #{caseId}
        </if>
        <if test="longitude != null">
            AND longitude = #{longitude}
        </if>
        <if test="latitude != null">
            AND latitude = #{latitude}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>