<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.formaldehyde.check_equipment">
    <resultMap id="checkEquipment" type="finley.gmair.model.formaldehyde.CheckEquipment">
        <result property="equipmentId" column="equipment_id"></result>
        <result property="equipmentName" column="equipment_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <insert id="insert" parameterType="finley.gmair.model.formaldehyde.CheckEquipment" useGeneratedKeys="false">
        INSERT INTO gmair_formaldehyde_detection.check_equipment(equipment_id, equipment_name, block_flag, create_time)
        VALUES(#{equipmentId}, #{equipmentName}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="checkEquipment">
        SELECT equipment_id,equipment_name,block_flag,create_time
        FROM gmair_formaldehyde_detection.check_equipment
        WHERE 1 = 1
        <if test="equipmentId != null">
            AND equipment_id = #{equipmentId}
        </if>
        <if test="equipmentName != null">
            AND equipment_name = #{equipmentName}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>