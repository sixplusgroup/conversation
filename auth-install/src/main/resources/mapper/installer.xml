<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gmair.install.worker">
    <resultMap id="memberVo" type="finley.gmair.model.installation.Member">
        <result property="memberId" column="member_id"></result>
        <result property="memberPhone" column="member_phone"></result>
        <result property="wechatId" column="wechat_id"></result>
        <result property="memberName" column="member_name"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
    </resultMap>

    <select id="query" parameterType="java.util.Map" resultMap="memberVo">
        SELECT member_id, member_phone, wechat_id, member_name, block_flag, create_time
        FROM gmair_install.team_member
        WHERE 1 = 1
        <if test="wechatId != null">
            AND wechat_id = #{wechatId}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>