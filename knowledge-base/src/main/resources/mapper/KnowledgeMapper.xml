<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="finley.gmair.dao.KnowledgeMapper">

    <insert id="insert" parameterType="finley.gmair.po.Knowledge" useGeneratedKeys="true" keyProperty="id">
        insert into knowledge (title, content, state, views, knowledge_type)
        values ( #{title}, #{content}, #{state}, #{views}, #{knowledge_type})
    </insert>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from knowledge where id = #{id}
    </delete>

    <update id="changeStatusTo2">
        update knowledge set status = 2 where id = #{id}
    </update>

    <update id="changeStatusTo1">
        update knowledge set status = 1 where id = #{id}
    </update>

    <select id="getById" resultMap="Knowledge">
        select * from knowledge where id = #{id}
    </select>

    <update id="modify" parameterType="finley.gmair.po.Knowledge">
        update knowledge
        set title=#{title},content=#{content},state=#{state}, views=#{views}, knowledge_type=#{knowledge_type}
        where id=#{id}
    </update>

    <resultMap id="Knowledge" type="finley.gmair.po.Knowledge"/>

</mapper>
